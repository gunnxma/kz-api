<tr>
    <td align="center"><%= department.name %></td>
    <td align="center">
    	<%= link_to "修改", edit_department_path(department) %>
      <%= link_to "删除", department_path(department), :method => :delete, data: { confirm: "确定要删除吗" } %>
    </td>
    </tr>